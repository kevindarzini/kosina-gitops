apiVersion: helm.toolkit.fluxcd.io/v2
kind: helmRelease
metadata:
  name: kosina-1-onboarding
  namespase: kosina
spec:
  interval: 30s
  chart:
    spec:
      chart:./helm/kosina
    sourceRef:
      kind: GitRepository
      name: kosina-app
      namespace: kosina
    interval: 30s
